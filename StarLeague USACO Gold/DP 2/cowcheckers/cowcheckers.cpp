// cowcheckers.cpp 

#include <iostream>
#include <set>
#include <utility>
using namespace std;
int rows, cols, t, diag(0);
set< pair<int,int> > pts;

void calcPts() {
	int n;
	if (rows >= cols) {
		n = rows;
	}
	else {
		n = cols;
	}

	for (int i = 0; i < n; i++) {
		int x, y;
		x = 

		//pair<int, int> coords = { 1,2 };
		//pts.insert(coords);

	}



}

int main()
{
	cin >> rows >> cols >> t;
	for (int i = 0; i < t; i++) {
		int x, y;
		cin >> x >> y;


	}

	

    return 0;
}